<div id="sign_up" class="content_pane">
  <h3>Update credentials</h3>
  <div class="form">
  <% form_for @user do |f| %>
    <%= f.error_messages %>
    <table>
      <tr><td>email:</td><td><%= @user.email %></td></tr>
      <tr><td>old or new password:</td><td><%= f.password_field :password %></td></tr>
      <tr><td>confirm password:</td><td><%= f.password_field :password_confirmation %></td></tr>
      <% if current_user.is_admin? %>
        <tr><td>admin user?:</td><td><%= f.check_box(:admin) %></td></tr>
      <% end %>
    </table>
    <%= f.submit 'Update' %>
  <% end %>
  </div>
</div>